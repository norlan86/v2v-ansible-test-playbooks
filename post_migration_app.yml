---
- name: Replace VMware with OpenStack
  hosts: all
  tasks:
    - name: Replace VMware with OpenStack
      replace:
        path: /var/www/html/index.html
        regexp: VMware
        replace: RedhatVirtualization
